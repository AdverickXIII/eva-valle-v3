# Dependencias de EJECUCION de la app (Streamlit Cloud lee este archivo).
# Solo paquetes que el codigo de app.py / core / ui / adapters importa
# directamente; el resto (altair, pyarrow, pillow, etc.) lo resuelve pip.
# Versiones fijadas a las mismas del entorno anterior (pip freeze).
streamlit==1.61.1
pandas==3.0.5
numpy==2.5.1
plotly==6.9.0
scikit-learn==1.9.0
scipy==1.18.0
statsmodels==0.14.6
matplotlib==3.11.1
reportlab==5.0.0
joblib==1.5.3
openpyxl==3.1.5
python-dotenv==1.2.2
